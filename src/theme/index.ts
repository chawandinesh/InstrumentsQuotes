import { createTheme } from '@mui/material';
import { green, purple,lightBlue } from '@mui/material/colors';

export const theme = createTheme({
  
  palette: {
    primary: {
      main: "#323232",
    },
    secondary: {
      main: lightBlue.A100,
    },
  },
});